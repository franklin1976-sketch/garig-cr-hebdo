{
  "name": "garig-cr-hebdo",
  "version": "1.2.0",
  "private": true,
  "description": "Compte rendu hebdomadaire d'exploitation Garig — enveloppe Android",
  "scripts": {
    "www": "mkdir -p www && cp index.html manifest.webmanifest sw.js www/ && cp -r icons www/",
    "android": "npm run www && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  }
}
